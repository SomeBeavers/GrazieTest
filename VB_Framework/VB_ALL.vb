'Initial value Is different Then the value after the first
'Call To the Function.

Public Class VB_ALL
    'I'm bit weak today. Not here apparently.
    ''' <summary>
    ''' Initial value Is different Then the value after the first
    ''' </summary>
    ''' <param name="a">This is a test parameter.</param>
    ''' <returns>This is a test return value.</returns>
    ''' <returns></returns>
    Function commetn()
        Dim myStringLiteral As String = "I'm bit weak today. Not here apparently. It is a test."
    End Function
End Class
